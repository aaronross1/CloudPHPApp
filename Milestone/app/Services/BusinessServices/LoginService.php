<?php
class LoginService
{
    /**
     * Checks to see if the user exists in the database and if so, verifies the password
     * @param $credentials
     */
    public function authorizeUser($credentials)
    {
        // Check if the users exists
        
        
        // Verify the correct password was inputted
    }
}